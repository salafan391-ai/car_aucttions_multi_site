{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml"
  },
  "deploy": {
    "startCommand": "python manage.py migrate_schemas --noinput && python manage.py collectstatic --noinput && gunicorn cars_multi_site.wsgi --log-file - --max-requests 500 --max-requests-jitter 50 --preload --workers 2 --threads 4 --worker-class gthread --timeout 29 --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
